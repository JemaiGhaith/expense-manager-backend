spring:
  security:
    enabled: false  # ⭐ Désactiver la sécurité pour les test
  mail:
    host: smtp.gmail.com
    port: 587
    username: datalake.rh@gmail.com
    password: yshwctetiasbkntj
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
  application:
    name: USER-MICROSERVICE  # Must be UPPERCASE to match gateway
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  web:
    resources:
      static-locations: file:./uploads/,classpath:/static/
  datasource:
    url: jdbc:postgresql://localhost:5432/expense_app
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC

server:
  port: 8083

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

management:
  endpoints:
    web:
      exposure:
        include: '*'

file:
  upload-dir: ./uploads


keycloak:
  server-url: http://localhost:8090
  realm: coral-io_realm
  client-id: admin-client
  client-secret: vov2JMbDjbDsZQCyJeBZIqcH2W5blsdD


logging:
  level:
    com.netflix.discovery: DEBUG  # Add this for Eureka debugging
    org.springframework.cloud.netflix: DEBUG